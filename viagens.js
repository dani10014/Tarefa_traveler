document.addEventListener('DOMContentLoaded', function() {
    const container = document.querySelector('.container-mae-todos');
    
    setTimeout(() => container.style.transform = 'translateY(0)', 200);
});